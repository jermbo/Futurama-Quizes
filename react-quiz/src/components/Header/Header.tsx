import React from "react";
import "./Header.scss";

const Header: React.FC = () => (
  <header>
    <i className="logo logo-image"></i>
    <h1 className="logo logo-text">Futurama Quiz App - HTML / CSS / JavaScript</h1>
  </header>
);

export default Header;
