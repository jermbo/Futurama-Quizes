import React from 'react';
import PropTypes from 'prop-types';
import './Quiz.scss';

const Quiz = () => (
  <div className="Quiz" data-testid="Quiz">
    Quiz Component
  </div>
);

Quiz.propTypes = {};

Quiz.defaultProps = {};

export default Quiz;
